# Requirements — `probability_estimator`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.335729+00:00.
> Edit the agent's source under `backend/app/agents/denial_forecaster/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `probability_estimator`
- **Parent:** `denial_forecaster`
- **Role:** denial_probability_calibration
- **Description:** Base-rate-calibrated payer-denial probability ∈ [0,1] anchored against MA / oncology-PA denial rates. Outputs estimator_confidence and a one-line summary.

## User stories

- **US-probability_estimator-1** As a payer-side reviewer, I want this agent to produce
  `ProbabilityEstimatorOutput` from `ProbabilityEstimatorInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-probability_estimator-2** As a compliance officer, I want every invocation of
  `probability_estimator` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-probability_estimator-3** As an oncologist or care-coordinator, I want
  `probability_estimator`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-probability_estimator-1** WHEN `probability_estimator` receives a `ProbabilityEstimatorInput` payload, IT SHALL
  emit a Pydantic-validated `ProbabilityEstimatorOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-probability_estimator-2** WHEN `probability_estimator` cannot produce a valid `ProbabilityEstimatorOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-probability_estimator-3** WHEN `probability_estimator` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-probability_estimator-4** WHEN any input guardrail returns `BLOCK`, `probability_estimator` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `probability_estimator` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock sonnet (reasoning) by default; fallback model `sonnet` on retry.

- Input schema:  `ProbabilityEstimatorInput` (Pydantic v2)
- Output schema: `ProbabilityEstimatorOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
