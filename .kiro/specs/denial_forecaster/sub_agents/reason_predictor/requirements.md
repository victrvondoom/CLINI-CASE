# Requirements — `reason_predictor`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.337253+00:00.
> Edit the agent's source under `backend/app/agents/denial_forecaster/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `reason_predictor`
- **Parent:** `denial_forecaster`
- **Role:** denial_reason_ranking
- **Description:** Up to 3 ranked payer denial rationales with policy-section pointers; empty when denial_probability < 0.15.

## User stories

- **US-reason_predictor-1** As a payer-side reviewer, I want this agent to produce
  `ReasonPredictorOutput` from `ReasonPredictorInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-reason_predictor-2** As a compliance officer, I want every invocation of
  `reason_predictor` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-reason_predictor-3** As an oncologist or care-coordinator, I want
  `reason_predictor`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-reason_predictor-1** WHEN `reason_predictor` receives a `ReasonPredictorInput` payload, IT SHALL
  emit a Pydantic-validated `ReasonPredictorOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-reason_predictor-2** WHEN `reason_predictor` cannot produce a valid `ReasonPredictorOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-reason_predictor-3** WHEN `reason_predictor` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-reason_predictor-4** WHEN any input guardrail returns `BLOCK`, `reason_predictor` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `reason_predictor` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock haiku (lightweight_extraction) by default; fallback model `sonnet` on retry.

- Input schema:  `ReasonPredictorInput` (Pydantic v2)
- Output schema: `ReasonPredictorOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
