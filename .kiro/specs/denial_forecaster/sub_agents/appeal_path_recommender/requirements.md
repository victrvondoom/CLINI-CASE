# Requirements — `appeal_path_recommender`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.339661+00:00.
> Edit the agent's source under `backend/app/agents/denial_forecaster/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `appeal_path_recommender`
- **Parent:** `denial_forecaster`
- **Role:** appeal_strategy_selection
- **Description:** Recommends the best appeal angle (enum) + KFF-baseline-calibrated overturn probability when denial probability ≥ 0.35; otherwise skipped with a reason.

## User stories

- **US-appeal_path_recommender-1** As a payer-side reviewer, I want this agent to produce
  `AppealPathRecommenderOutput` from `AppealPathRecommenderInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-appeal_path_recommender-2** As a compliance officer, I want every invocation of
  `appeal_path_recommender` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-appeal_path_recommender-3** As an oncologist or care-coordinator, I want
  `appeal_path_recommender`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-appeal_path_recommender-1** WHEN `appeal_path_recommender` receives a `AppealPathRecommenderInput` payload, IT SHALL
  emit a Pydantic-validated `AppealPathRecommenderOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-appeal_path_recommender-2** WHEN `appeal_path_recommender` cannot produce a valid `AppealPathRecommenderOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-appeal_path_recommender-3** WHEN `appeal_path_recommender` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-appeal_path_recommender-4** WHEN any input guardrail returns `BLOCK`, `appeal_path_recommender` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `appeal_path_recommender` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock haiku (lightweight_extraction) by default; fallback model `sonnet` on retry.

- Input schema:  `AppealPathRecommenderInput` (Pydantic v2)
- Output schema: `AppealPathRecommenderOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
