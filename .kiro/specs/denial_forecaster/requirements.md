# Requirements — `denial_forecaster`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.334027+00:00.
> Edit the agent's source under `backend/app/agents/denial_forecaster/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `denial_forecaster`
- **Parent:** `(top-level)`
- **Role:** denial_forecast_orchestration
- **Description:** Predicts the *payer's* denial probability + top likely reasons + recommended appeal angle (KFF-2024 calibrated).

## User stories

- **US-denial_forecaster-1** As a payer-side reviewer, I want this agent to produce
  `DenialForecasterOutput` from `DenialForecasterInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-denial_forecaster-2** As a compliance officer, I want every invocation of
  `denial_forecaster` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-denial_forecaster-3** As an oncologist or care-coordinator, I want
  `denial_forecaster`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-denial_forecaster-1** WHEN `denial_forecaster` receives a `DenialForecasterInput` payload, IT SHALL
  emit a Pydantic-validated `DenialForecasterOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-denial_forecaster-2** WHEN `denial_forecaster` cannot produce a valid `DenialForecasterOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-denial_forecaster-3** WHEN `denial_forecaster` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-denial_forecaster-4** WHEN any input guardrail returns `BLOCK`, `denial_forecaster` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `denial_forecaster` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `DenialForecasterInput` (Pydantic v2)
- Output schema: `DenialForecasterOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
