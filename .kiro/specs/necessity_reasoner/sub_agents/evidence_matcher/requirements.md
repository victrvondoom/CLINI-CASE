# Requirements — `evidence_matcher`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.345821+00:00.
> Edit the agent's source under `backend/app/agents/necessity_reasoner/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `evidence_matcher`
- **Parent:** `necessity_reasoner`
- **Role:** criterion_evaluation
- **Description:** For ONE atomic criterion, decides MET / NOT_MET / AMBIGUOUS against the ClinicalSnapshot with cited supporting evidence. Self-grades and retries if the grader flags hallucinated facts or weak evidence.

## User stories

- **US-evidence_matcher-1** As a payer-side reviewer, I want this agent to produce
  `EvidenceMatch` from `EvidenceMatcherInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-evidence_matcher-2** As a compliance officer, I want every invocation of
  `evidence_matcher` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-evidence_matcher-3** As an oncologist or care-coordinator, I want
  `evidence_matcher`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-evidence_matcher-1** WHEN `evidence_matcher` receives a `EvidenceMatcherInput` payload, IT SHALL
  emit a Pydantic-validated `EvidenceMatch` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-evidence_matcher-2** WHEN `evidence_matcher` cannot produce a valid `EvidenceMatch` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-evidence_matcher-3** WHEN `evidence_matcher` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-evidence_matcher-4** WHEN any input guardrail returns `BLOCK`, `evidence_matcher` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `evidence_matcher` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock sonnet (reasoning) by default; fallback model `sonnet` on retry.

- Input schema:  `EvidenceMatcherInput` (Pydantic v2)
- Output schema: `EvidenceMatch` (Pydantic v2)
- Max iterations: 3
- Quality threshold: 0.8
