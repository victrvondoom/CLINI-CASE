# Requirements — `confidence_calibrator`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.347808+00:00.
> Edit the agent's source under `backend/app/agents/necessity_reasoner/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `confidence_calibrator`
- **Parent:** `necessity_reasoner`
- **Role:** confidence_aggregation
- **Description:** Calibrates per-criterion confidence ∈ [0,1] and aggregates to overall (deterministic min — enforced post-LLM). Drives the HITL gate threshold.

## User stories

- **US-confidence_calibrator-1** As a payer-side reviewer, I want this agent to produce
  `ConfidenceCalibratorOutput` from `ConfidenceCalibratorInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-confidence_calibrator-2** As a compliance officer, I want every invocation of
  `confidence_calibrator` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-confidence_calibrator-3** As an oncologist or care-coordinator, I want
  `confidence_calibrator`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-confidence_calibrator-1** WHEN `confidence_calibrator` receives a `ConfidenceCalibratorInput` payload, IT SHALL
  emit a Pydantic-validated `ConfidenceCalibratorOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-confidence_calibrator-2** WHEN `confidence_calibrator` cannot produce a valid `ConfidenceCalibratorOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-confidence_calibrator-3** WHEN `confidence_calibrator` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-confidence_calibrator-4** WHEN any input guardrail returns `BLOCK`, `confidence_calibrator` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `confidence_calibrator` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock haiku (lightweight_extraction) by default; fallback model `sonnet` on retry.

- Input schema:  `ConfidenceCalibratorInput` (Pydantic v2)
- Output schema: `ConfidenceCalibratorOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
