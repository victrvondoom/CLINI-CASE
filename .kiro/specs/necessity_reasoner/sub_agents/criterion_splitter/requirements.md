# Requirements — `criterion_splitter`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.343763+00:00.
> Edit the agent's source under `backend/app/agents/necessity_reasoner/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `criterion_splitter`
- **Parent:** `necessity_reasoner`
- **Role:** policy_atomization
- **Description:** Splits multi-clause policy criteria into atomic, individually-checkable statements with type tags (inclusion/exclusion) and section pointers.

## User stories

- **US-criterion_splitter-1** As a payer-side reviewer, I want this agent to produce
  `CriterionSplitterOutput` from `CriterionSplitterInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-criterion_splitter-2** As a compliance officer, I want every invocation of
  `criterion_splitter` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-criterion_splitter-3** As an oncologist or care-coordinator, I want
  `criterion_splitter`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-criterion_splitter-1** WHEN `criterion_splitter` receives a `CriterionSplitterInput` payload, IT SHALL
  emit a Pydantic-validated `CriterionSplitterOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-criterion_splitter-2** WHEN `criterion_splitter` cannot produce a valid `CriterionSplitterOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-criterion_splitter-3** WHEN `criterion_splitter` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-criterion_splitter-4** WHEN any input guardrail returns `BLOCK`, `criterion_splitter` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `criterion_splitter` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock sonnet (reasoning) by default; fallback model `sonnet` on retry.

- Input schema:  `CriterionSplitterInput` (Pydantic v2)
- Output schema: `CriterionSplitterOutput` (Pydantic v2)
- Max iterations: 3
- Quality threshold: 0.0
