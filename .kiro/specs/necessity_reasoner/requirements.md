# Requirements — `necessity_reasoner`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.341937+00:00.
> Edit the agent's source under `backend/app/agents/necessity_reasoner/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `necessity_reasoner`
- **Parent:** `(top-level)`
- **Role:** necessity_orchestration
- **Description:** Per-criterion MET/NOT_MET/AMBIGUOUS evaluation with calibrated confidence. Parallel fan-out across atomic criteria. Drives the HITL gate.

## User stories

- **US-necessity_reasoner-1** As a payer-side reviewer, I want this agent to produce
  `NecessityReasonerOutput` from `NecessityReasonerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-necessity_reasoner-2** As a compliance officer, I want every invocation of
  `necessity_reasoner` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-necessity_reasoner-3** As an oncologist or care-coordinator, I want
  `necessity_reasoner`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-necessity_reasoner-1** WHEN `necessity_reasoner` receives a `NecessityReasonerInput` payload, IT SHALL
  emit a Pydantic-validated `NecessityReasonerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-necessity_reasoner-2** WHEN `necessity_reasoner` cannot produce a valid `NecessityReasonerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-necessity_reasoner-3** WHEN `necessity_reasoner` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-necessity_reasoner-4** WHEN any input guardrail returns `BLOCK`, `necessity_reasoner` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `necessity_reasoner` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `NecessityReasonerInput` (Pydantic v2)
- Output schema: `NecessityReasonerOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
