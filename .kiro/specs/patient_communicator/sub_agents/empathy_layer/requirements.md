# Requirements — `empathy_layer`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.351124+00:00.
> Edit the agent's source under `backend/app/agents/patient_communicator/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `empathy_layer`
- **Parent:** `patient_communicator`
- **Role:** patient_voice_synthesis
- **Description:** Picks tone (reassuring/neutral/urgent) by verdict and writes a patient-facing headline + 2–4 paragraph body in plain language.

## User stories

- **US-empathy_layer-1** As a payer-side reviewer, I want this agent to produce
  `EmpathyLayerOutput` from `EmpathyLayerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-empathy_layer-2** As a compliance officer, I want every invocation of
  `empathy_layer` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-empathy_layer-3** As an oncologist or care-coordinator, I want
  `empathy_layer`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-empathy_layer-1** WHEN `empathy_layer` receives a `EmpathyLayerInput` payload, IT SHALL
  emit a Pydantic-validated `EmpathyLayerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-empathy_layer-2** WHEN `empathy_layer` cannot produce a valid `EmpathyLayerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-empathy_layer-3** WHEN `empathy_layer` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-empathy_layer-4** WHEN any input guardrail returns `BLOCK`, `empathy_layer` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `empathy_layer` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock sonnet (patient_voice) by default; fallback model `sonnet` on retry.

- Input schema:  `EmpathyLayerInput` (Pydantic v2)
- Output schema: `EmpathyLayerOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
