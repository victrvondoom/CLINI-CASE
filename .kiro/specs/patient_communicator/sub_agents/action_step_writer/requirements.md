# Requirements — `action_step_writer`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.354856+00:00.
> Edit the agent's source under `backend/app/agents/patient_communicator/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `action_step_writer`
- **Parent:** `patient_communicator`
- **Role:** next_step_generation
- **Description:** Generates up to 5 concrete next-step imperatives with timing tags (today/this_week/this_month/after_decision).

## User stories

- **US-action_step_writer-1** As a payer-side reviewer, I want this agent to produce
  `ActionStepWriterOutput` from `ActionStepWriterInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-action_step_writer-2** As a compliance officer, I want every invocation of
  `action_step_writer` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-action_step_writer-3** As an oncologist or care-coordinator, I want
  `action_step_writer`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-action_step_writer-1** WHEN `action_step_writer` receives a `ActionStepWriterInput` payload, IT SHALL
  emit a Pydantic-validated `ActionStepWriterOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-action_step_writer-2** WHEN `action_step_writer` cannot produce a valid `ActionStepWriterOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-action_step_writer-3** WHEN `action_step_writer` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-action_step_writer-4** WHEN any input guardrail returns `BLOCK`, `action_step_writer` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `action_step_writer` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock haiku (lightweight_extraction) by default; fallback model `sonnet` on retry.

- Input schema:  `ActionStepWriterInput` (Pydantic v2)
- Output schema: `ActionStepWriterOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
