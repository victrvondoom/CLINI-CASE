# Requirements — `reading_level_tuner`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.352934+00:00.
> Edit the agent's source under `backend/app/agents/patient_communicator/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `reading_level_tuner`
- **Parent:** `patient_communicator`
- **Role:** reading_level_enforcement
- **Description:** Pure-Python Flesch-Kincaid grade calculator + banned-phrase substitution. Enforces ≤7th-grade reading level on patient-facing copy.

## User stories

- **US-reading_level_tuner-1** As a payer-side reviewer, I want this agent to produce
  `ReadingLevelTunerOutput` from `ReadingLevelTunerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-reading_level_tuner-2** As a compliance officer, I want every invocation of
  `reading_level_tuner` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-reading_level_tuner-3** As an oncologist or care-coordinator, I want
  `reading_level_tuner`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-reading_level_tuner-1** WHEN `reading_level_tuner` receives a `ReadingLevelTunerInput` payload, IT SHALL
  emit a Pydantic-validated `ReadingLevelTunerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-reading_level_tuner-2** WHEN `reading_level_tuner` cannot produce a valid `ReadingLevelTunerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-reading_level_tuner-3** WHEN `reading_level_tuner` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-reading_level_tuner-4** WHEN any input guardrail returns `BLOCK`, `reading_level_tuner` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `reading_level_tuner` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `ReadingLevelTunerInput` (Pydantic v2)
- Output schema: `ReadingLevelTunerOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
