# Requirements — `verdict_synthesizer`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.327887+00:00.
> Edit the agent's source under `backend/app/agents/decision_composer/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `verdict_synthesizer`
- **Parent:** `decision_composer`
- **Role:** deterministic_verdict_rule
- **Description:** Maps a NecessityAssessment to APPROVE / DENY / REFER and emits a full audit trace of which rule fired. No LLM. Microsecond latency. Byte-for-byte replayable.

## User stories

- **US-verdict_synthesizer-1** As a payer-side reviewer, I want this agent to produce
  `VerdictSynthesizerOutput` from `VerdictSynthesizerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-verdict_synthesizer-2** As a compliance officer, I want every invocation of
  `verdict_synthesizer` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-verdict_synthesizer-3** As an oncologist or care-coordinator, I want
  `verdict_synthesizer`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-verdict_synthesizer-1** WHEN `verdict_synthesizer` receives a `VerdictSynthesizerInput` payload, IT SHALL
  emit a Pydantic-validated `VerdictSynthesizerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-verdict_synthesizer-2** WHEN `verdict_synthesizer` cannot produce a valid `VerdictSynthesizerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-verdict_synthesizer-3** WHEN `verdict_synthesizer` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-verdict_synthesizer-4** WHEN any input guardrail returns `BLOCK`, `verdict_synthesizer` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `verdict_synthesizer` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `VerdictSynthesizerInput` (Pydantic v2)
- Output schema: `VerdictSynthesizerOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
