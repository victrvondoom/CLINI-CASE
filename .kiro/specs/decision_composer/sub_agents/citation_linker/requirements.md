# Requirements — `citation_linker`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.332228+00:00.
> Edit the agent's source under `backend/app/agents/decision_composer/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `citation_linker`
- **Parent:** `decision_composer`
- **Role:** citation_chain
- **Description:** Builds the citation chain so every factual claim in the rationale points to either a clinical evidence resource or a policy excerpt.

## User stories

- **US-citation_linker-1** As a payer-side reviewer, I want this agent to produce
  `CitationLinkerOutput` from `CitationLinkerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-citation_linker-2** As a compliance officer, I want every invocation of
  `citation_linker` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-citation_linker-3** As an oncologist or care-coordinator, I want
  `citation_linker`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-citation_linker-1** WHEN `citation_linker` receives a `CitationLinkerInput` payload, IT SHALL
  emit a Pydantic-validated `CitationLinkerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-citation_linker-2** WHEN `citation_linker` cannot produce a valid `CitationLinkerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-citation_linker-3** WHEN `citation_linker` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-citation_linker-4** WHEN any input guardrail returns `BLOCK`, `citation_linker` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `citation_linker` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock haiku (lightweight_extraction) by default; fallback model `sonnet` on retry.

- Input schema:  `CitationLinkerInput` (Pydantic v2)
- Output schema: `CitationLinkerOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
