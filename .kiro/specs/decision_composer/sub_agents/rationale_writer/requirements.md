# Requirements — `rationale_writer`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.329648+00:00.
> Edit the agent's source under `backend/app/agents/decision_composer/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `rationale_writer`
- **Parent:** `decision_composer`
- **Role:** executive_rationale
- **Description:** Composes a 2–4 sentence executive rationale paragraph that justifies the (already-determined) verdict using snapshot fields and policy phrases.

## User stories

- **US-rationale_writer-1** As a payer-side reviewer, I want this agent to produce
  `RationaleWriterOutput` from `RationaleWriterInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-rationale_writer-2** As a compliance officer, I want every invocation of
  `rationale_writer` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-rationale_writer-3** As an oncologist or care-coordinator, I want
  `rationale_writer`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-rationale_writer-1** WHEN `rationale_writer` receives a `RationaleWriterInput` payload, IT SHALL
  emit a Pydantic-validated `RationaleWriterOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-rationale_writer-2** WHEN `rationale_writer` cannot produce a valid `RationaleWriterOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-rationale_writer-3** WHEN `rationale_writer` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-rationale_writer-4** WHEN any input guardrail returns `BLOCK`, `rationale_writer` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `rationale_writer` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock sonnet (reasoning) by default; fallback model `sonnet` on retry.

- Input schema:  `RationaleWriterInput` (Pydantic v2)
- Output schema: `RationaleWriterOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
