# Requirements — `decision_composer`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.325731+00:00.
> Edit the agent's source under `backend/app/agents/decision_composer/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `decision_composer`
- **Parent:** `(top-level)`
- **Role:** decision_orchestration
- **Description:** Deterministic verdict + LLM-written rationale + LLM-validated citation chain.

## User stories

- **US-decision_composer-1** As a payer-side reviewer, I want this agent to produce
  `DecisionComposerOutput` from `DecisionComposerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-decision_composer-2** As a compliance officer, I want every invocation of
  `decision_composer` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-decision_composer-3** As an oncologist or care-coordinator, I want
  `decision_composer`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-decision_composer-1** WHEN `decision_composer` receives a `DecisionComposerInput` payload, IT SHALL
  emit a Pydantic-validated `DecisionComposerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-decision_composer-2** WHEN `decision_composer` cannot produce a valid `DecisionComposerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-decision_composer-3** WHEN `decision_composer` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-decision_composer-4** WHEN any input guardrail returns `BLOCK`, `decision_composer` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `decision_composer` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `DecisionComposerInput` (Pydantic v2)
- Output schema: `DecisionComposerOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
