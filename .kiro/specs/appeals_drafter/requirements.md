# Requirements — `appeals_drafter`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.309338+00:00.
> Edit the agent's source under `backend/app/agents/appeals_drafter/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `appeals_drafter`
- **Parent:** `(top-level)`
- **Role:** appeals_orchestration
- **Description:** On DENY: drafts a NCCN-citing formal appeal letter + structured arguments JSON for payer-API submission.

## User stories

- **US-appeals_drafter-1** As a payer-side reviewer, I want this agent to produce
  `AppealsDrafterOutput` from `AppealsDrafterInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-appeals_drafter-2** As a compliance officer, I want every invocation of
  `appeals_drafter` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-appeals_drafter-3** As an oncologist or care-coordinator, I want
  `appeals_drafter`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-appeals_drafter-1** WHEN `appeals_drafter` receives a `AppealsDrafterInput` payload, IT SHALL
  emit a Pydantic-validated `AppealsDrafterOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-appeals_drafter-2** WHEN `appeals_drafter` cannot produce a valid `AppealsDrafterOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-appeals_drafter-3** WHEN `appeals_drafter` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-appeals_drafter-4** WHEN any input guardrail returns `BLOCK`, `appeals_drafter` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `appeals_drafter` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `AppealsDrafterInput` (Pydantic v2)
- Output schema: `AppealsDrafterOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
