# Requirements — `counter_evidence_finder`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.311697+00:00.
> Edit the agent's source under `backend/app/agents/appeals_drafter/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `counter_evidence_finder`
- **Parent:** `appeals_drafter`
- **Role:** counter_evidence_extraction
- **Description:** For each contested criterion, extracts payer position + counter position + supporting evidence quotations from the snapshot.

## User stories

- **US-counter_evidence_finder-1** As a payer-side reviewer, I want this agent to produce
  `CounterEvidenceFinderOutput` from `CounterEvidenceFinderInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-counter_evidence_finder-2** As a compliance officer, I want every invocation of
  `counter_evidence_finder` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-counter_evidence_finder-3** As an oncologist or care-coordinator, I want
  `counter_evidence_finder`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-counter_evidence_finder-1** WHEN `counter_evidence_finder` receives a `CounterEvidenceFinderInput` payload, IT SHALL
  emit a Pydantic-validated `CounterEvidenceFinderOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-counter_evidence_finder-2** WHEN `counter_evidence_finder` cannot produce a valid `CounterEvidenceFinderOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-counter_evidence_finder-3** WHEN `counter_evidence_finder` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-counter_evidence_finder-4** WHEN any input guardrail returns `BLOCK`, `counter_evidence_finder` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `counter_evidence_finder` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock sonnet (reasoning) by default; fallback model `sonnet` on retry.

- Input schema:  `CounterEvidenceFinderInput` (Pydantic v2)
- Output schema: `CounterEvidenceFinderOutput` (Pydantic v2)
- Max iterations: 3
- Quality threshold: 0.8
