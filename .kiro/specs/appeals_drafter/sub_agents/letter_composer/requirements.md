# Requirements — `letter_composer`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.315099+00:00.
> Edit the agent's source under `backend/app/agents/appeals_drafter/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `letter_composer`
- **Parent:** `appeals_drafter`
- **Role:** appeal_letter_composition
- **Description:** Produces a ~600-word formal appeal letter (5-paragraph structure) plus AppealArgument JSON entries for payer-API submission.

## User stories

- **US-letter_composer-1** As a payer-side reviewer, I want this agent to produce
  `LetterComposerOutput` from `LetterComposerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-letter_composer-2** As a compliance officer, I want every invocation of
  `letter_composer` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-letter_composer-3** As an oncologist or care-coordinator, I want
  `letter_composer`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-letter_composer-1** WHEN `letter_composer` receives a `LetterComposerInput` payload, IT SHALL
  emit a Pydantic-validated `LetterComposerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-letter_composer-2** WHEN `letter_composer` cannot produce a valid `LetterComposerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-letter_composer-3** WHEN `letter_composer` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-letter_composer-4** WHEN any input guardrail returns `BLOCK`, `letter_composer` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `letter_composer` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock sonnet (reasoning) by default; fallback model `sonnet` on retry.

- Input schema:  `LetterComposerInput` (Pydantic v2)
- Output schema: `LetterComposerOutput` (Pydantic v2)
- Max iterations: 3
- Quality threshold: 0.8
