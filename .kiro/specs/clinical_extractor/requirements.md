# Requirements — `clinical_extractor`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.316835+00:00.
> Edit the agent's source under `backend/app/agents/clinical_extractor/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `clinical_extractor`
- **Parent:** `(top-level)`
- **Role:** fhir_to_snapshot_orchestration
- **Description:** Parses FHIR R4 bundle + physician note into a strictly-typed ClinicalSnapshot. PHI sanitised at the prompt boundary.

## User stories

- **US-clinical_extractor-1** As a payer-side reviewer, I want this agent to produce
  `ClinicalExtractorOutput` from `ClinicalExtractorInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-clinical_extractor-2** As a compliance officer, I want every invocation of
  `clinical_extractor` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-clinical_extractor-3** As an oncologist or care-coordinator, I want
  `clinical_extractor`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-clinical_extractor-1** WHEN `clinical_extractor` receives a `ClinicalExtractorInput` payload, IT SHALL
  emit a Pydantic-validated `ClinicalExtractorOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-clinical_extractor-2** WHEN `clinical_extractor` cannot produce a valid `ClinicalExtractorOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-clinical_extractor-3** WHEN `clinical_extractor` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-clinical_extractor-4** WHEN any input guardrail returns `BLOCK`, `clinical_extractor` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `clinical_extractor` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `ClinicalExtractorInput` (Pydantic v2)
- Output schema: `ClinicalExtractorOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
