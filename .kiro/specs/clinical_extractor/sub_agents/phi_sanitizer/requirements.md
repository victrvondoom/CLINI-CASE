# Requirements — `phi_sanitizer`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.321308+00:00.
> Edit the agent's source under `backend/app/agents/clinical_extractor/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `phi_sanitizer`
- **Parent:** `clinical_extractor`
- **Role:** phi_masking
- **Description:** Pattern-based PII mask emitting Bedrock-Guardrails-compatible output shape. On Bedrock deployment this is replaced 1-for-1 by the Guardrail's PII filter.

## User stories

- **US-phi_sanitizer-1** As a payer-side reviewer, I want this agent to produce
  `PHISanitizerOutput` from `PHISanitizerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-phi_sanitizer-2** As a compliance officer, I want every invocation of
  `phi_sanitizer` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-phi_sanitizer-3** As an oncologist or care-coordinator, I want
  `phi_sanitizer`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-phi_sanitizer-1** WHEN `phi_sanitizer` receives a `PHISanitizerInput` payload, IT SHALL
  emit a Pydantic-validated `PHISanitizerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-phi_sanitizer-2** WHEN `phi_sanitizer` cannot produce a valid `PHISanitizerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-phi_sanitizer-3** WHEN `phi_sanitizer` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-phi_sanitizer-4** WHEN any input guardrail returns `BLOCK`, `phi_sanitizer` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `phi_sanitizer` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `PHISanitizerInput` (Pydantic v2)
- Output schema: `PHISanitizerOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
