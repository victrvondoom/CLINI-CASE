# Requirements — `fhir_resource_validator`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.319649+00:00.
> Edit the agent's source under `backend/app/agents/clinical_extractor/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `fhir_resource_validator`
- **Parent:** `clinical_extractor`
- **Role:** structural_validation
- **Description:** Validates FHIR R4 Bundle structure (Patient + Condition required; counts Observation / MedicationRequest / DiagnosticReport). Errors short-circuit the parent before any LLM call is made.

## User stories

- **US-fhir_resource_validator-1** As a payer-side reviewer, I want this agent to produce
  `FHIRResourceValidatorOutput` from `FHIRResourceValidatorInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-fhir_resource_validator-2** As a compliance officer, I want every invocation of
  `fhir_resource_validator` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-fhir_resource_validator-3** As an oncologist or care-coordinator, I want
  `fhir_resource_validator`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-fhir_resource_validator-1** WHEN `fhir_resource_validator` receives a `FHIRResourceValidatorInput` payload, IT SHALL
  emit a Pydantic-validated `FHIRResourceValidatorOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-fhir_resource_validator-2** WHEN `fhir_resource_validator` cannot produce a valid `FHIRResourceValidatorOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-fhir_resource_validator-3** WHEN `fhir_resource_validator` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-fhir_resource_validator-4** WHEN any input guardrail returns `BLOCK`, `fhir_resource_validator` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `fhir_resource_validator` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `FHIRResourceValidatorInput` (Pydantic v2)
- Output schema: `FHIRResourceValidatorOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
