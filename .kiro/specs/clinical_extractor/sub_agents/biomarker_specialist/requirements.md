# Requirements — `biomarker_specialist`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.322776+00:00.
> Edit the agent's source under `backend/app/agents/clinical_extractor/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `biomarker_specialist`
- **Parent:** `clinical_extractor`
- **Role:** biomarker_extraction
- **Description:** Extracts a focused, treatment-relevant set of high-stakes oncology biomarkers (HER2, EGFR, BRCA1/2, MSI, PD-L1, ALK, ROS1, BRAF, ECOG, LVEF) from FHIR + note.

## User stories

- **US-biomarker_specialist-1** As a payer-side reviewer, I want this agent to produce
  `BiomarkerSpecialistOutput` from `BiomarkerSpecialistInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-biomarker_specialist-2** As a compliance officer, I want every invocation of
  `biomarker_specialist` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-biomarker_specialist-3** As an oncologist or care-coordinator, I want
  `biomarker_specialist`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-biomarker_specialist-1** WHEN `biomarker_specialist` receives a `BiomarkerSpecialistInput` payload, IT SHALL
  emit a Pydantic-validated `BiomarkerSpecialistOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-biomarker_specialist-2** WHEN `biomarker_specialist` cannot produce a valid `BiomarkerSpecialistOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-biomarker_specialist-3** WHEN `biomarker_specialist` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-biomarker_specialist-4** WHEN any input guardrail returns `BLOCK`, `biomarker_specialist` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `biomarker_specialist` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock haiku (lightweight_extraction) by default; fallback model `sonnet` on retry.

- Input schema:  `BiomarkerSpecialistInput` (Pydantic v2)
- Output schema: `BiomarkerSpecialistOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
