# Requirements — `policy_retriever`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.356585+00:00.
> Edit the agent's source under `backend/app/agents/policy_retriever/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `policy_retriever`
- **Parent:** `(top-level)`
- **Role:** policy_retrieval_orchestration
- **Description:** Fetches the top-5 payer-specific PA policy sections relevant to this case from the 21-policy corpus (Bedrock KB in production).

## User stories

- **US-policy_retriever-1** As a payer-side reviewer, I want this agent to produce
  `PolicyRetrieverOutput` from `PolicyRetrieverInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-policy_retriever-2** As a compliance officer, I want every invocation of
  `policy_retriever` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-policy_retriever-3** As an oncologist or care-coordinator, I want
  `policy_retriever`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-policy_retriever-1** WHEN `policy_retriever` receives a `PolicyRetrieverInput` payload, IT SHALL
  emit a Pydantic-validated `PolicyRetrieverOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-policy_retriever-2** WHEN `policy_retriever` cannot produce a valid `PolicyRetrieverOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-policy_retriever-3** WHEN `policy_retriever` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-policy_retriever-4** WHEN any input guardrail returns `BLOCK`, `policy_retriever` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `policy_retriever` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `PolicyRetrieverInput` (Pydantic v2)
- Output schema: `PolicyRetrieverOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
