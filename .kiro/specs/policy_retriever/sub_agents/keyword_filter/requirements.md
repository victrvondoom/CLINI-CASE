# Requirements — `keyword_filter`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.358197+00:00.
> Edit the agent's source under `backend/app/agents/policy_retriever/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `keyword_filter`
- **Parent:** `policy_retriever`
- **Role:** candidate_selection
- **Description:** Iterates the curated 21-policy corpus and returns all (policy, section) pairs whose payer matches and whose treatment_keywords fuzzy-match the requested treatment name. Pure Python, no LLM.

## User stories

- **US-keyword_filter-1** As a payer-side reviewer, I want this agent to produce
  `KeywordFilterOutput` from `KeywordFilterInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-keyword_filter-2** As a compliance officer, I want every invocation of
  `keyword_filter` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-keyword_filter-3** As an oncologist or care-coordinator, I want
  `keyword_filter`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-keyword_filter-1** WHEN `keyword_filter` receives a `KeywordFilterInput` payload, IT SHALL
  emit a Pydantic-validated `KeywordFilterOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-keyword_filter-2** WHEN `keyword_filter` cannot produce a valid `KeywordFilterOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-keyword_filter-3** WHEN `keyword_filter` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-keyword_filter-4** WHEN any input guardrail returns `BLOCK`, `keyword_filter` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `keyword_filter` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `KeywordFilterInput` (Pydantic v2)
- Output schema: `KeywordFilterOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
