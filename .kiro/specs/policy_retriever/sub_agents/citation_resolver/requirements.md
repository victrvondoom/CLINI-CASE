# Requirements — `citation_resolver`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.362014+00:00.
> Edit the agent's source under `backend/app/agents/policy_retriever/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `citation_resolver`
- **Parent:** `policy_retriever`
- **Role:** citation_pointer_resolution
- **Description:** Deterministic mapping from ranked candidate indices to fully-pointered PolicyExcerpt objects (source URL, page number, section heading, decreasing relevance score). No LLM.

## User stories

- **US-citation_resolver-1** As a payer-side reviewer, I want this agent to produce
  `CitationResolverOutput` from `CitationResolverInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-citation_resolver-2** As a compliance officer, I want every invocation of
  `citation_resolver` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-citation_resolver-3** As an oncologist or care-coordinator, I want
  `citation_resolver`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-citation_resolver-1** WHEN `citation_resolver` receives a `CitationResolverInput` payload, IT SHALL
  emit a Pydantic-validated `CitationResolverOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-citation_resolver-2** WHEN `citation_resolver` cannot produce a valid `CitationResolverOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-citation_resolver-3** WHEN `citation_resolver` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-citation_resolver-4** WHEN any input guardrail returns `BLOCK`, `citation_resolver` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `citation_resolver` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `CitationResolverInput` (Pydantic v2)
- Output schema: `CitationResolverOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
