# Requirements — `llm_reranker`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-05-02T13:05:55.360274+00:00.
> Edit the agent's source under `backend/app/agents/policy_retriever/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `llm_reranker`
- **Parent:** `policy_retriever`
- **Role:** relevance_rerank
- **Description:** Cross-encoder LLM rerank — fires only when keyword_filter returns >5 candidates. Outputs the top-K most-relevant indices for the case.

## User stories

- **US-llm_reranker-1** As a payer-side reviewer, I want this agent to produce
  `LLMRerankerOutput` from `LLMRerankerInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-llm_reranker-2** As a compliance officer, I want every invocation of
  `llm_reranker` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-llm_reranker-3** As an oncologist or care-coordinator, I want
  `llm_reranker`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-llm_reranker-1** WHEN `llm_reranker` receives a `LLMRerankerInput` payload, IT SHALL
  emit a Pydantic-validated `LLMRerankerOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-llm_reranker-2** WHEN `llm_reranker` cannot produce a valid `LLMRerankerOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-llm_reranker-3** WHEN `llm_reranker` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-llm_reranker-4** WHEN any input guardrail returns `BLOCK`, `llm_reranker` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `llm_reranker` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - LLM-backed via Bedrock sonnet (reasoning) by default; fallback model `sonnet` on retry.

- Input schema:  `LLMRerankerInput` (Pydantic v2)
- Output schema: `LLMRerankerOutput` (Pydantic v2)
- Max iterations: 2
- Quality threshold: 0.0
