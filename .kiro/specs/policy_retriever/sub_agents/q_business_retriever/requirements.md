# Requirements — `q_business_retriever`

> Generated from ClinCase `AGENT_MANIFEST` at 2026-09-19T01:25:19.531023+00:00.
> Edit the agent's source under `backend/app/agents/policy_retriever/` —
> this file is regenerated by `python -m app.integrations.kiro.exporter`.

## Identity

- **Name:** `q_business_retriever`
- **Parent:** `policy_retriever`
- **Role:** candidate_selection
- **Description:** Amazon Q Business semantic-search backend for policy candidate selection. Same input/output as keyword_filter; engaged when USE_AMAZON_Q=true. Surfaces ranked passages from the customer's existing M365 / SharePoint / Confluence policy library — no separate vector index required.

## User stories

- **US-q_business_retriever-1** As a payer-side reviewer, I want this agent to produce
  `KeywordFilterOutput` from `KeywordFilterInput` so that the determination chain is
  reproducible and auditable per CMS-0057-F § IV.D.

- **US-q_business_retriever-2** As a compliance officer, I want every invocation of
  `q_business_retriever` to write an `agent_runs` row with input/output/model_id/tokens
  so that any decision can be reconstructed within 12 seconds.

- **US-q_business_retriever-3** As an oncologist or care-coordinator, I want
  `q_business_retriever`'s output to cite specific policy sections (or clinical evidence)
  so that the rationale satisfies CMS-0057-F § IV.B.2 specific-reason
  notice and aligns with NCCN guidelines.

## EARS acceptance criteria

- **EARS-q_business_retriever-1** WHEN `q_business_retriever` receives a `KeywordFilterInput` payload, IT SHALL
  emit a Pydantic-validated `KeywordFilterOutput` within the per-agent budget
  ceiling (USD ≤ $5/case shared budget; tokens bounded per invocation).

- **EARS-q_business_retriever-2** WHEN `q_business_retriever` cannot produce a valid `KeywordFilterOutput` after
  `max_iterations` attempts, IT SHALL raise `AgentExhausted` rather than
  return malformed output.

- **EARS-q_business_retriever-3** WHEN `q_business_retriever` runs as a sub-agent (parent != null), IT
  SHALL share the parent's `AgentContext` so cost/token/latency budgets
  are accounted at the case level.

- **EARS-q_business_retriever-4** WHEN any input guardrail returns `BLOCK`, `q_business_retriever` SHALL
  raise `InputBlocked` and emit a `guardrail_blocked` span event.

## Compliance hooks

- CMS-0057-F § IV.D — every invocation persisted to `agent_runs`.
- HIPAA — PHI guardrails redact PHI from prompts to non-Bedrock LLMs.
- CA SB 1120 — adverse determinations route through `review_gate` (not this agent's responsibility, but `q_business_retriever` MUST emit `triggered_hitl` correctly when low-confidence).

## Implementation notes

  - Deterministic Python implementation; no LLM call.

- Input schema:  `KeywordFilterInput` (Pydantic v2)
- Output schema: `KeywordFilterOutput` (Pydantic v2)
- Max iterations: 1
- Quality threshold: 0.0
